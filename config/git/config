[user]
    name = Dilyn Corner
    email = dilyn.corner@canonical.com
    signingkey = E8B89D944BF37F70F58493436CC15833EF3198A7
[init]
    defaultBranch = main
[core]
    symlink = true
	editor = hx
	pager = cat
[credential]
    helper = cache --timeout=3600
[commit]
    gpgsign = true
    verbose = true
[pull]
    rebase = true
[rebase]
    stat = true
    autosquash = true
    conflictstyle = diff3
[color]
    ui = always
    diff = always
    grep = always
    pager = true
    status = always
    branch = always
    decorate = always
    interactive = always
[color "diff"]
    new = green
    old = red
    meta = yellow bold
    frag = magenta bold
[color "status"]
    added = green
    changed = cyan
    untracked = red bold
[color "branch"]
    local = cyan
    remote = red
    current = green
[merge]
    conflictstyle = diff3
[rerere]
    enabled = 1
[alias]
    st = status
    re = remote -v
    br = branch -vv
    bl = blame --color-lines --color-by-age
    adog = log --all --decorate --oneline --graph
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
[pager]
	diff = less -FX
