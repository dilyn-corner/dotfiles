#!/bin/sh

## ksh specific
# Scrollback with arrow keys
set -o emacs
alias __A=`echo "\020"` # up arrow = ^p = back a command
alias __B=`echo "\016"` # down arrow = ^n = down a command
alias __C=`echo "\006"` # right arrow = ^f = forward a character
alias __D=`echo "\002"` # left arrow = ^b = back a character
alias __H=`echo "\001"` # home = ^a = start of line
alias __Y=`echo "\005"` # end = ^e = end of line


## Pretty shell
PS1='${PWD} -> '
export HISTFILE=/home/dilyn/.ksh_history


## Aliases!
alias github="pash s github.com/dilyn-corner"
alias email="pash s tutanota.com/dilyn.corner"
alias ix="curl -F 'f:1=<-' ix.io"
alias dotties='/usr/bin/git --git-dir=${HOME}/git/dotfiles/ --work-tree=${HOME}'


## Colors
# paleta 
cat "$HOME/.cache/current-palette"


## Functions
# Automate commiting
git () {
    case "$1" in
        commit) /usr/bin/git commit -S ;;
        *) /usr/bin/git "$@" ;;
    esac
}
